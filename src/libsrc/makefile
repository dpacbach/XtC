ifndef TOPLEVELWD
include $(dir $(lastword $(MAKEFILE_LIST)))../makefile
.DEFAULT_GOAL := $(DEFAULT_GOAL_LIBRARY)
else

LOCATION_LIB_INT := $(CWD)interface

LIB_INTERFACE := $(CWD)interface

$(call enter,src)

endif
